name: Deploy to GitHub Pages

on:
  push:
    branches:
      master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: deborah-digges/deploy-storybook-action@v1.0
        with:
          access-token: ${{ secrets.ACCESS_TOKEN }}

