name: Deploy to GitHub Pages

on:
  push:
    branches:
      master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: deborah-digges/deploy-storybook-action@v1.1
        with:
          access-token: ${{ secrets.ACCESS_TOKEN }}

